<h1>Eat - Da - Burgers!</h1>

<h2>Burgers that are not eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{> burgers/burger-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have already been eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/burger-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Add your Own Burger:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="eaten"> Have you eaten it or not?</label><br>
    <input type="radio" name="eaten" value="1" checked> It's Eaten!<br>
    <input type="radio" name="eaten" value="0"> It's Not Eaten
  </div>

  <button type="submit">Add this Burger</button>
</form>
